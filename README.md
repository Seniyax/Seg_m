# Semantic Segmentation with CAMvid Data
Semantic segmentation's goal is to classify each pixel in an image into a specific category like in image classification where we assign a specific category for the whole image.
